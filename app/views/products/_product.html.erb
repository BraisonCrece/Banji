<div class="card w-[97%] p-3 border-solid border-1 border-gray-300">
  <%= link_to  product_path(product) do %>
    <div class="product_image first:mt-3">
      <%= image_tag product.images[0] if product.images.attached? %>
    </div>
    <div class="product_info">
      <h2><%= product.title %></h2>
      <p><%= product.price %>€</p>
    </div>
  <% end %>
</div>
